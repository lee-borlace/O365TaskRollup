Here are the commands used to set up package.json for NPM. Do these from Node.js command prompt inside the project (not solution). You need to create package.json from the command line first using 
npm init.

npm install concurrently --save-dev
npm install @angular/core --save